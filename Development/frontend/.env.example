# Leave empty to use the Vite dev proxy (recommended for local dev)
# Set to backend URL for production if serving frontend & backend from different origins
# e.g. VITE_API_BASE=https://api.william.app
VITE_API_BASE=http://localhost:3001
